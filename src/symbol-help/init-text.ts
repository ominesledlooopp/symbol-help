export const init_text = `
# ${'the init command synopsys'.magenta().strong().underline()}

## the init command is the first action to take to initialize your project faster.

### ${'flags:'.strong().underline()}

- ${'--bare'.color(33)} initialize an empty project
- ${'--name={string}'.color(33)} initialize a project with name=xyz
`;
